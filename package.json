{
    "name": "open-catalyst-data-studio",
    "version": "0.1.0",
    "private": true,
    "license": "MIT",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "yarn workspaces run build",
        "dev": "yarn workspaces run dev",
        "lint": "eslint . --ext .ts,.tsx",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "eslint": "^8.56.0",
        "prettier": "^3.2.4",
        "typescript": "^5.3.3"
    }
}